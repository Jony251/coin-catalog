# ✅ Чеклист безопасности перед коммитом

## Перед каждым коммитом проверьте:

### 🔐 Секреты и пароли

- [ ] Файл `.env` НЕ добавлен в коммит
- [ ] В коде нет хардкод паролей/токенов
- [ ] В коде нет connection strings с паролями
- [ ] Все секреты в `.env` или переменных окружения
- [ ] `.env.example` не содержит реальных паролей

### 📁 Файлы

- [ ] `.gitignore` содержит `.env`
- [ ] `.gitignore` содержит `*.pem`, `*.key`, `*.cert`
- [ ] Нет файлов `credentials/`, `secrets/`
- [ ] Нет файлов с расширением `.pem`, `.key`

### 📝 Документация

- [ ] В README.md нет реальных паролей
- [ ] В SQL скриптах нет реальных connection strings
- [ ] В примерах кода используются плейсхолдеры

### 🔍 Проверка Git

```bash
# Проверьте статус
git status

# Убедитесь что .env не в списке
# Если есть - удалите:
git rm --cached .env

# Проверьте diff
git diff --cached

# Поищите пароли в изменениях
git diff --cached | grep -i "password\|secret\|token"
```

## Перед первым пушем в GitHub

### 🔍 Проверка истории

```bash
# Поиск .env в истории
git log --all --full-history --source -- .env

# Поиск паролей в истории
git log -p | grep -i "password\|secret\|DATABASE_URL"
```

### 🧹 Если нашли пароли в истории

**НЕ ПУШЬТЕ!** Сначала очистите историю:

```bash
# Вариант 1: BFG Repo-Cleaner (рекомендуется)
# https://rtyley.github.io/bfg-repo-cleaner/

# Вариант 2: git-filter-repo
# https://github.com/newren/git-filter-repo

# Вариант 3: Пересоздайте репозиторий
```

## После пуша в GitHub

### 🔐 Проверьте репозиторий

1. Откройте https://github.com/Jony251/coin-catalog
2. Проверьте файлы:
   - [ ] `.env` отсутствует
   - [ ] `.env.example` есть (без паролей)
   - [ ] `SECURITY.md` есть
3. Проверьте коммиты - нет ли паролей в истории

### 🚨 Если пароль утёк

**НЕМЕДЛЕННО:**

1. **Смените пароль в Neon**
   - Откройте https://console.neon.tech/
   - Settings → Reset Password

2. **Обновите .env локально**
   ```bash
   # Обновите DATABASE_URL с новым паролем
   ```

3. **Сгенерируйте новый JWT Secret**
   ```bash
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```

4. **Очистите Git историю**
   - Используйте BFG Repo-Cleaner
   - Или пересоздайте репозиторий

5. **Force push**
   ```bash
   git push --force origin main
   ```

6. **Уведомите команду**
   - Все должны обновить свои локальные копии

## Регулярные проверки

### Еженедельно

- [ ] Проверьте `.gitignore` актуален
- [ ] Проверьте нет ли новых секретов в коде
- [ ] Обновите зависимости (`npm audit`)

### Ежемесячно

- [ ] Смените JWT Secret
- [ ] Проверьте логи безопасности в Neon
- [ ] Проверьте активные сессии пользователей

### Перед релизом

- [ ] Полный аудит безопасности
- [ ] Проверка всех переменных окружения
- [ ] Тестирование на уязвимости
- [ ] Обновление всех зависимостей

## Инструменты для проверки

### git-secrets

```bash
# Установка
brew install git-secrets  # macOS
# или
apt-get install git-secrets  # Linux

# Настройка
git secrets --install
git secrets --register-aws
```

### truffleHog

```bash
# Поиск секретов в истории
trufflehog git https://github.com/Jony251/coin-catalog
```

### npm audit

```bash
# Проверка уязвимостей в зависимостях
npm audit

# Автоматическое исправление
npm audit fix
```

## Контакты

При обнаружении уязвимости:
- 📧 Email: security@yourproject.com
- 🔒 НЕ создавайте публичный issue
- ⏰ Дайте 90 дней на исправление

---

**Помните:** Один утёкший пароль может скомпрометировать весь проект!

## Полезные ссылки

- [GitHub: Removing sensitive data](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [BFG Repo-Cleaner](https://rtyley.github.io/bfg-repo-cleaner/)
- [git-secrets](https://github.com/awslabs/git-secrets)
