# ⚡ Установка БД прямо сейчас (5 минут)

## Шаг 1: Откройте Neon Console

Перейдите по ссылке: https://console.neon.tech/

## Шаг 2: Откройте SQL Editor

1. Выберите ваш проект
2. В левом меню нажмите **SQL Editor**

## Шаг 3: Скопируйте и выполните schema.sql

1. Откройте файл `database/schema.sql` в вашем редакторе
2. Нажмите **Ctrl+A** (выделить всё)
3. Нажмите **Ctrl+C** (скопировать)
4. Вернитесь в SQL Editor на Neon
5. Нажмите **Ctrl+V** (вставить)
6. Нажмите **Run** (или Ctrl+Enter)

**Ожидайте ~10 секунд** пока выполнится скрипт.

## Шаг 4: Проверка

Выполните этот запрос в SQL Editor:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

**Должны увидеть:**
- sync_log
- user_coins
- user_sessions
- users

## Шаг 5: Добавить тестовые данные (опционально)

1. Откройте файл `database/seed.sql`
2. Скопируйте содержимое (Ctrl+A, Ctrl+C)
3. Вставьте в SQL Editor (Ctrl+V)
4. Нажмите **Run**

## ✅ Готово!

Теперь проверьте данные:

```sql
-- Посмотреть пользователей
SELECT * FROM users;

-- Посмотреть статистику
SELECT * FROM user_collection_stats;
```

## 🎉 База данных готова к использованию!

Следующие шаги:
1. ✅ БД создана
2. ⏳ Создать API сервер (см. документацию)
3. ⏳ Обновить UserCollectionService
4. ⏳ Добавить авторизацию

---

**Возникли проблемы?**
- Убедитесь, что вы вошли в правильный проект
- Проверьте, что у вас есть права на создание таблиц
- Если таблицы уже существуют, можно их удалить и создать заново
