# ✅ Исправление ошибки "no such column: periodId"

## Что было исправлено:

### 1. **DatabaseService.js**
- Изменена схема таблицы `rulers`: поле `periodId` теперь **опциональное** (убран `NOT NULL`)
- Увеличена версия БД до **4** для автоматической миграции
- Обновлена логика миграции: таблицы пересоздаются при обновлении до версии 4

### 2. **Ruler.js**
- В конструкторе `periodId` теперь опциональный: `this.periodId = data.periodId || null`

## 🔄 Что нужно сделать:

### Вариант 1: Перезапустить приложение (рекомендуется)

```bash
# Остановите приложение (Ctrl+C)
# Запустите заново
npm start
```

При следующем запуске:
1. DatabaseService обнаружит старую версию БД (3)
2. Автоматически выполнит миграцию до версии 4
3. Пересоздаст таблицы с правильной схемой
4. Заполнит данными

### Вариант 2: Очистить кеш и БД

Если автоматическая миграция не сработает:

```bash
# Остановите приложение
# Очистите кеш Expo
npx expo start --clear

# Или удалите БД вручную на устройстве/эмуляторе
# Android: /data/data/com.coincatalog.app/databases/coin_catalog.db
# iOS: Library/LocalDatabase/coin_catalog.db
```

### Вариант 3: Переустановить приложение

На эмуляторе/устройстве:
1. Удалите приложение
2. Запустите заново: `npm run android`

## 📊 Проверка исправления:

После перезапуска приложение должно:
- ✅ Загрузиться без ошибок
- ✅ Показать список правителей
- ✅ Открывать детали правителя
- ✅ Показывать монеты

## 🔍 Что изменилось в схеме БД:

### Было:
```sql
CREATE TABLE rulers (
  id TEXT PRIMARY KEY,
  periodId TEXT NOT NULL,  -- ❌ Обязательное поле
  ...
);
```

### Стало:
```sql
CREATE TABLE rulers (
  id TEXT PRIMARY KEY,
  periodId TEXT,  -- ✅ Опциональное поле
  ...
);
```

## 💡 Почему это произошло:

1. В каталоге данных (`data/catalog.js`) у правителей нет поля `periodId`
2. DatabaseService добавлял `periodId: 'russian_empire'` при загрузке
3. Но схема БД требовала `NOT NULL`, что вызывало конфликт
4. Теперь `periodId` опциональный и проблема решена

## 🎯 Следующие шаги:

После исправления ошибки:
1. ✅ Приложение работает
2. ⏳ Скачайте изображения правителей (см. `assets/images/DOWNLOAD_INSTRUCTIONS.md`)
3. ⏳ Протестируйте все функции
4. ⏳ Добавьте авторизацию и синхронизацию

---

**Ошибка исправлена!** 🎉 Перезапустите приложение.
