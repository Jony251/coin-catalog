# 🚀 Руководство по настройке

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка Neon Database

### 2.1 Создание базы данных

1. Перейдите на https://neon.tech
2. Зарегистрируйтесь или войдите
3. Нажмите **"Create Project"**
4. Выберите регион (ближайший к вам)
5. Скопируйте **Connection String**

### 2.2 Настройка .env

Откройте файл `.env` в корне проекта и вставьте вашу строку подключения:

```env
DATABASE_URL=postgresql://username:password@ep-xxx-xxx.neon.tech/dbname?sslmode=require
```

### 2.3 Создание таблиц

1. Откройте [Neon Console](https://console.neon.tech)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Откройте файл `database/schema.sql` в вашем проекте
5. Скопируйте весь SQL код
6. Вставьте в SQL Editor
7. Нажмите **"Run"**

✅ Таблицы созданы!

## Шаг 3: Запуск API сервера

Откройте **новый терминал** и запустите:

```bash
npm run server
```

Вы должны увидеть:
```
🚀 API Server running on http://localhost:3000
📊 Health check: http://localhost:3000/api/health
```

### Проверка работы сервера

Откройте в браузере: http://localhost:3000/api/health

Должен вернуть:
```json
{
  "status": "ok",
  "database": "connected"
}
```

## Шаг 4: Запуск мобильного приложения

Откройте **второй терминал** и запустите:

```bash
npm start
```

Затем:
- **Android:** Нажмите `a` или отсканируйте QR-код в Expo Go
- **iOS:** Нажмите `i` или отсканируйте QR-код в Expo Go
- **Web:** Нажмите `w`

## Шаг 5: Тестирование регистрации

1. В приложении перейдите в **Профиль**
2. Нажмите **"Зарегистрироваться"**
3. Заполните форму:
   - Email: `test@example.com`
   - Пароль: `123456`
   - Имя: `Тестовый пользователь`
4. Нажмите **"Зарегистрироваться"**

### Проверка в базе данных

1. Откройте Neon Console → SQL Editor
2. Выполните запрос:

```sql
SELECT * FROM users;
```

Вы должны увидеть нового пользователя! ✅

## Шаг 6: Тестирование коллекции

1. Добавьте монету в коллекцию
2. Проверьте в базе данных:

```sql
SELECT * FROM user_coins;
```

Монета должна быть там! ✅

## 🔧 Устранение проблем

### Сервер не запускается

**Проблема:** `Error: connect ECONNREFUSED`

**Решение:**
1. Проверьте `DATABASE_URL` в `.env`
2. Убедитесь что Neon DB доступна
3. Проверьте интернет соединение

### Приложение не подключается к серверу

**Для Android (реальное устройство):**

В `app.json` измените:
```json
"extra": {
  "apiUrl": "http://192.168.X.X:3000"
}
```

Где `192.168.X.X` - это IP вашего компьютера в локальной сети.

**Узнать IP:**
```bash
# Windows
ipconfig

# Mac/Linux
ifconfig
```

### База данных не обновляется

1. Проверьте что сервер запущен
2. Проверьте логи сервера в терминале
3. Проверьте логи приложения (в Expo)

## 📱 Для продакшена

### 1. Измените JWT_SECRET

В `.env`:
```env
JWT_SECRET=ваш-очень-длинный-случайный-секретный-ключ-минимум-32-символа
```

### 2. Разверните API на хостинге

Рекомендуемые платформы:
- **Vercel** (бесплатно)
- **Railway** (бесплатно)
- **Render** (бесплатно)

### 3. Обновите API_URL

В `app.json`:
```json
"extra": {
  "apiUrl": "https://your-api.vercel.app"
}
```

## ✅ Готово!

Теперь ваше приложение:
- ✅ Регистрирует пользователей в базе данных
- ✅ Сохраняет коллекцию на сервере
- ✅ Синхронизируется между устройствами
- ✅ Защищено JWT токенами
- ✅ Работает офлайн (fallback)
