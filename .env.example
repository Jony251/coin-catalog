# Neon Database Connection
# Получите connection string на https://console.neon.tech/
DATABASE_URL=postgresql://username:password@host.neon.tech/dbname?sslmode=require

# API Configuration
API_URL=http://localhost:3000
API_PORT=3000

# JWT Secret (для авторизации)
# Сгенерируйте случайную строку: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here-change-in-production

# App Configuration
APP_ENV=development

# ============================================
# ВАЖНО: Скопируйте этот файл в .env и заполните реальными значениями
# НЕ коммитьте файл .env в Git!
# ============================================
